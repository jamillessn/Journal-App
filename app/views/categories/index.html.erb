
<div class="h-100 w-full flex-col items-center justify-center bg-teal-lightest font-sans">
    <div class=" max-w-screen-xl m-4 flex-grow p-4">
        <h1 class="text-3xl font-bold">My Categories</h1>
        
            <% @categories.each do |category|%>
            <div class="bg-white rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg">
                <p> <%= link_to category.title, category %> </p>
                <p>  Description: <%= category.desc %> </p>
            </div>
            <% end %>

            <%= button_tag(type: "submit", class: " bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded inline-flex items-center") do %>
                <%= inline_svg "svg/plus.svg", class: "h-4 w-4 inline mr-2" %>
                <%= link_to "Add a new category", new_category_path, class: "ml-2" %>
            <% end %>
    </div>
                
</div>

